Submissions Detail -
Reverse Linked List
Accepted
28 / 28 testcases passed
Arisha
Arisha
submitted at Aug 15, 2026 14:16
Solution
Runtime 0ms Beats 100.00% Memory 13.38 MB Beats 72.87%
Code
C++
/**
 * Definition for singly-linked list.
 * struct ListNode {
 *     int val;
 *     ListNode *next;
 *     ListNode() : val(0), next(nullptr) {}
 *     ListNode(int x) : val(x), next(nullptr) {}
 *     ListNode(int x, ListNode *next) : val(x), next(next) {}
 * };
 */
class Solution {
public:
    ListNode* reverseList(ListNode* head) {
        ListNode*curr=head;
         ListNode*nxt=nullptr;
        ListNode*prev=nullptr;
        while(curr!=nullptr){
        nxt=curr->next;
        curr->next=prev;
        prev=curr;
        curr=nxt;
        }
        head=prev;
        return head;
    }
};
